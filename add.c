#include <stdio.h>
int main() {    
    int number2,number,1 number2, sum;
    ring the chnages in branch1
    
    printf("Enter two integer: ");
    scanf("%d %d", &number1, &number2);

    // calculate the sum
    sum = number1 + number2;      
    
    printf("%d + %d = %d", number1, number2, sum);
    return 0;
}

